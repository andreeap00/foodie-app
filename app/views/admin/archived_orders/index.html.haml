%h1 Archived Orders

%table
  %thead
    %tr
      %th Order Date
      %th Status
      %th Actions

  %tbody
    - @archived_orders.each do |archived_order|
      %tr
        %td= archived_order.created_at.strftime('%Y-%m-%d')
        %td= archived_order.status
        %td= link_to 'View Products', admin_archived_order_path
        